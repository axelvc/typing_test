import styled from 'styled-components'

export default styled.div`
  padding: var(--space-md);
  border: 0.25rem solid var(--color-bg-alt);
  border-radius: var(--rounded);
  box-sizing: border-box;
`
